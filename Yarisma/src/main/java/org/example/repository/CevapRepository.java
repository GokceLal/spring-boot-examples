package org.example.repository;

import org.example.entity.Cevap;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CevapRepository extends JpaRepository<Cevap,Long> {
}
